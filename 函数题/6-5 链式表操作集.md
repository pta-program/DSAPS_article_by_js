## 6-5 链式表操作集

- **分值：** 20分

## 题目描述

本题要求实现链式表的操作集。

## 函数接口定义
```javascript
// JavaScript 函数接口
function Find(L, X) {}
function Insert(L, X, P) {}
function Delete(L, P) {}
```

其中`List`结构定义如下：

```javascript
const ERROR = null;
class Node {
  constructor(data, next = null) {
    this.Data = data;
    this.Next = next;
  }
}
```

各个操作函数的定义为：

`Position Find( List L, ElementType X )`：返回线性表中首次出现X的位置。若找不到则返回ERROR；

`List Insert( List L, ElementType X, Position P )`：将X插入在位置P指向的结点之前，返回链表的表头。如果参数P指向非法位置，则打印“Wrong Position for Insertion”，返回ERROR；

`List Delete( List L, Position P )`：将位置P的元素删除并返回链表的表头。若参数P指向非法位置，则打印“Wrong Position for Deletion”并返回ERROR。

## 裁判测试程序样例
```javascript
let L = null;
for (const x of [1, 2, 3]) L = Insert(L, x, L);
const p = Find(L, 2);
L = Delete(L, p);
console.log([L.Data, L.Next.Data].join(" "));
```

## 输入样例
```
6
12 2 4 87 10 2
4
2 12 87 5
```

## 输出样例
```
2 is found and deleted.
12 is found and deleted.
87 is found and deleted.
Finding Error: 5 is not in.
5 is inserted as the last element.
Wrong Position for Insertion
Wrong Position for Deletion
10 4 2 5
```

## 函数部分

```javascript
// 插入/删除前验证 P 是否确实属于当前链表，避免错误指针破坏结构。
function Find(L, X) {
  for (let p = L; p; p = p.Next) if (p.Data === X) return p;
  return ERROR;
}
function Insert(L, X, P) {
  if (P !== null && P !== L && !contains(L, P)) {
    console.log("Wrong Position for Insertion");
    return ERROR;
  }
  const node = new Node(X, P);
  if (P === L) return node;
  if (P === null) {
    if (!L) return node;
    let tail = L;
    while (tail.Next) tail = tail.Next;
    tail.Next = node;
    return L;
  }
  let prev = L;
  while (prev.Next !== P) prev = prev.Next;
  prev.Next = node;
  return L;
}
function contains(L, target) {
  for (let p = L; p; p = p.Next) if (p === target) return true;
  return false;
}
function Delete(L, P) {
  if (!P || !contains(L, P)) {
    console.log("Wrong Position for Deletion");
    return ERROR;
  }
  if (L === P) return L.Next;
  let prev = L;
  while (prev.Next !== P) prev = prev.Next;
  prev.Next = P.Next;
  return L;
}
```

## 解题思路

链表不带头结点，因此插入或删除表头时需要单独更新表头指针。`Find` 直接顺序扫描；`Insert` 根据 `P` 的取值分别处理表头、表尾和中间位置，并先确认 `P` 确实属于当前链表；`Delete` 通过寻找前驱结点把目标结点跳过后释放。非法位置统一输出题目规定的信息并返回 `ERROR`。

## 代码流程说明

1. `Find` 从 `L` 开始扫描，首次匹配即返回结点指针。
2. `Insert`：`P == L` 时插入表头，`P == NULL` 时遍历到表尾，其他情况寻找 `P` 的前驱后插入。
3. `Delete`：`P == L` 时删除表头，其他情况寻找前驱并调整链接；找不到前驱则报错。
4. 成功修改链表后返回新的或原来的表头。

## 代码流程图

```mermaid
flowchart TD
    A[调用链表操作] --> B{操作类型}
    B -- Find --> C[从 L 顺序扫描]
    C --> D{找到 X}
    D -- 是 --> E[返回结点]
    D -- 否 --> F[返回 ERROR]
    B -- Insert --> G{P 是否为空}
    G -- 是 --> H[遍历到表尾并追加]
    G -- 否 --> I{P 是否为表头}
    I -- 是 --> J[新结点成为表头]
    I -- 否 --> K[寻找 P 的前驱]
    K --> L{前驱存在}
    L -- 否 --> M[报错并返回 ERROR]
    L -- 是 --> N[在前驱和 P 之间插入]
    B -- Delete --> O{P 是否为表头}
    O -- 是 --> P[表头后移并释放 P]
    O -- 否 --> Q[寻找 P 的前驱]
    Q --> R{前驱存在}
    R -- 否 --> S[报错并返回 ERROR]
    R -- 是 --> T[跳过并释放 P]
```

## 解题流程图

```mermaid
flowchart TD
    A[建立链表] --> B[读取操作请求]
    B --> C[查找目标位置]
    C --> D{插入或删除位置是否合法}
    D -- 否 --> E[输出错误信息]
    D -- 是 --> F[调整指针并释放或申请结点]
    E --> B
    F --> B
```
